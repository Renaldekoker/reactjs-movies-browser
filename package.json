{
  "name": "movies-browser",
  "version": "0.1.0",
  "description": "A small app to display a list of movies",
  "private": true,
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "serve-prod": "npm run build && cd build && php -S 0.0.0.0:80"
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "keywords": [
    "Movies",
    "Browser",
    "SPA"
  ],
  "author": "Renaldo van Dyk",
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-scripts": "4.0.3"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
